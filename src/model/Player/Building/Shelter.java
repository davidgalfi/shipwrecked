package model.Player.Building;

public class Shelter extends Building{

    // Properties:
    private boolean have_shelter = false; // false

    // Getters and setters:
    // have shelter:
    public boolean isHave_shelter() {
        return have_shelter;
    }
    public void setHave_shelter(boolean have_shelter) {
        this.have_shelter = have_shelter;
    }
}
